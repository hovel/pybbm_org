dependencies:
  pre:
    - pip install -r build/pipreq.txt -U
test:
  override:
    - ./manage.py test pybbm_org --noinput
deployment:
  production:
    branch: master
    commands:
      - ssh zeus@${DEPLOY_HOST} /home/zeus/deploy.sh pybbm_org production ${ANSIBLE_PASSWORD}:
          timeout: 600
